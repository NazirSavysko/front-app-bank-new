# Feature Branch Enumeration

This document lists all branches under the `feature/` prefix (child branches) for both repositories in the Bank project.

---

## Repository: `front-app-bank-new` (Frontend)

| # | Branch Name | Notes |
|---|-------------|-------|
| — | *(none)* | No `feature/*` child branches exist |

**All branches in this repository:**

| Branch | Starts with `feature/`? |
|--------|------------------------|
| `master` | ✗ |
| `feature` | ✗ (plain `feature`, not a `feature/<name>` child branch) |
| `copilot/featureremove-emojis-localization-ua` | ✗ |
| `copilot/list-feature-branches` | ✗ |

> **Conclusion:** There are currently **no** child branches following the `feature/<name>` naming convention in the frontend repository. Only a single branch named exactly `feature` (without a sub-path) exists.

---

## Repository: `bank-rest-api-app` (Backend)

| # | Branch Name | Notes |
|---|-------------|-------|
| — | *(none)* | No `feature/*` child branches exist |

**All branches in this repository:**

| Branch | Starts with `feature/`? |
|--------|------------------------|
| `master` | ✗ |
| `feature` | ✗ (plain `feature`, not a `feature/<name>` child branch) |
| `BP-53-Backend-Сависько-Назір` | ✗ |
| `BP-58-Backend-Ткачик-Вʼячеслав` | ✗ |
| `BP-59-Backend-Булгакова-Юля` | ✗ |
| `codex/implement-paginated-transaction-history` | ✗ |
| `copilot/implement-unified-transaction-history` | ✗ |

> **Conclusion:** There are currently **no** child branches following the `feature/<name>` naming convention in the backend repository. Only a single branch named exactly `feature` (without a sub-path) exists.

---

## Summary

| Repository | `feature/` child branches found |
|------------|--------------------------------|
| `front-app-bank-new` (Frontend) | **0** |
| `bank-rest-api-app` (Backend) | **0** |

Neither repository has branches that follow the `feature/<name>` child-branch pattern. Both repositories contain a branch named exactly `feature` (without a trailing slash or sub-path), but this is not a child branch under a `feature/` prefix — it is a standalone branch.

---

*Last updated: 2026-03-07*
